/*
* surveyReducers.js
* */
import {

} from "./actionsTypes";

const initialState = {};

const surveyReducers= (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default surveyReducers;
/*
* surveyResultsReducers.js
* */
import {

} from "./actionsTypes";

const initialState = {};

const surveyResultsReducers= (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default surveyResultsReducers;